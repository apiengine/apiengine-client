/**
 * You can override this by the name of the config file you wish to load if necessary.
 * When unspecified, the config file matching the hostname of the machine the client is running on will be
 * loaded instead.
 *
 * For example, to load config/s.apiengine.io.js as your app configuration, return "s.apiengine.io".
 */
define([], function() {
	return '';
});
